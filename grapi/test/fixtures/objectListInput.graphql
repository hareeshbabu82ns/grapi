
type Note {
    language: String!
    text: String!
    score: Int!
}

type User @Model(dataSource: "memory", key: "users_object_list"){
    id: ID! @unique @autoGen
    name: String! @unique
    notes: [ Note! ]!
}
